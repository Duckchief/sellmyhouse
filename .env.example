# Database
DATABASE_URL="postgresql://smhn:smhn_dev@localhost:5432/sellmyhomenow_dev"
DATABASE_URL_TEST="postgresql://smhn:smhn_test@localhost:5433/sellmyhomenow_test"

# Session
SESSION_SECRET="change-me-to-a-random-64-char-string"

# AI Providers (only active provider key required)
AI_ANTHROPIC_API_KEY=""
AI_OPENAI_API_KEY=""
AI_GOOGLE_API_KEY=""

# WhatsApp
WHATSAPP_TOKEN=""
WHATSAPP_PHONE_ID=""
WHATSAPP_VERIFY_TOKEN=""
# Meta WhatsApp Business API webhook verification token (required)
# Webhook signature verification is unconditional — server will not start
# without this value set
WHATSAPP_WEBHOOK_VERIFY_TOKEN=""

# Email (SMTP)
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@sellmyhomenow.sg"

# Encryption
ENCRYPTION_KEY=""

# JWT — dedicated secret for unsubscribe tokens and other JWT operations
# Must be a separate 32+ byte random string — never reuse SESSION_SECRET
JWT_SECRET=""

# HDB Data
DATAGOV_API_KEY=""

# App
NODE_ENV="development"
PORT=3000
BASE_URL="http://localhost:3000"
