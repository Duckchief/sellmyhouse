services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    volumes:
      - pgdata_dev:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: sellmyhomenow_dev
      POSTGRES_USER: smhn
      POSTGRES_PASSWORD: smhn_dev
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U smhn"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata_dev:
